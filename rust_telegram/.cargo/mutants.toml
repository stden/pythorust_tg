# cargo-mutants configuration for the Rust toolchain

# Skip thin CLI entrypoints to keep mutation runs focused on the core library logic.
exclude_globs = ["src/bin/**"]

# Give slower async tests some headroom and store reports under target/mutants.
timeout_multiplier = 2.0
minimum_test_timeout = 30
output = "target/mutants"
test_tool = "cargo"
