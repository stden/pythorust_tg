# Чтение чатов Telegram и автоответчик

Python-инструменты для чтения истории чатов, отслеживания реакций и автоматических ответов.

## 📋 Обзор проекта
- Экспорт сообщений Telegram в markdown-файлы
- Отслеживание реакций и метрик вовлечённости
- Автоответы через OpenAI/AI-интеграции
- Управление несколькими чатами и сессиями

## 🚀 Возможности

### Чтение чатов (`read.py`)
- Экспорт истории чата в markdown (до 3000 сообщений)
- Подсчёт реакций и метрик вовлечённости
- Скачивание медиа из популярных сообщений (>100k реакций)
- Автоудаление сообщений с низкой вовлечённостью
- Поддержка личных чатов и каналов

### Автоответчик (`autoanswer.py`)
- Автоматические ответы через OpenAI API
- Мониторинг новых сообщений в реальном времени
- Настраиваемые системные промпты
- Управление сессиями через Telethon

### Простой экспорт (`tg.py`)
- Упрощённый экспорт чата
- Настраиваемый лимит сообщений (по умолчанию 200)
- Скачивание медиа для сообщений с 1000+ реакций
- Отображение реакций и эмодзи

### Дополнительные утилиты
- 🤖 **AI-консультант с RAG** (`ai_project_consultant.py`) — интерактивный режим и Telegram-бот, ищет ответы в `knowledge_base/`
- 🛠️ **Task Assistant Bot** (`task_assistant_bot.py`) — управление N8N, бэкапами и быстрые команды
- 🔍 **N8N Monitor** (`n8n_monitor.py`, `n8n_monitor.service`) — health-check и автоперезапуск
- 💾 **N8N Backup** (`n8n_backup.py`, `n8n_backup_cron.sh`) — бэкапы и ротация
- 🛒 **BFL Sales Bot** (`bfl_sales_bot.py`) — AI-продавец массажных кресел (MySQL-хранилище сессий)
- 🤝 **Credit Expert Bot** (`credit_expert_bot.py`) — консультант по долгам, сохраняет диалоги в MySQL
- 🧠 **Graph RAG анализ** (`analyze_with_lightrag.py`) — строит LightRAG граф из MySQL (`telegram_messages` + реакции)
- 🔌 **MCP сервер** (`mcp_telegram_server.py`) — FastMCP-тулы: список чатов, чтение последних сообщений и отправка ответа из IDE/агентов
- 🔎 **Qdrant** — векторная база данных для семантического поиска (порт 6333)
- 🌸 **Hara Site** (`hara_site/`) — сайт сообщества с профилями участников

Подробности: `DASHA_TOOLS.md`.

## 📦 Зависимости

```
telethon
openai
python-dotenv
pyyaml
requests
aiohttp
mcp
behave
pytest
kurigram      # Pyrogram fork (опционально)
boto3         # S3/бэкапы (опционально)
```

Установка:
```bash
pip install -r requirements.txt
```

## 🔧 Настройка

### 1. Подготовьте .env
```bash
cp .env.example .env
nano .env
```

Минимально заполните:
- `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `TELEGRAM_PHONE` (получить на https://my.telegram.org/)
- `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` — для алертов из `n8n_monitor.py`
- `TASK_ASSISTANT_BOT_TOKEN` и/или `AI_CONSULTANT_BOT_TOKEN` — для ботов
- AI-провайдеры: `OPENAI_API_KEY` (или `ANTHROPIC_API_KEY` / `GOOGLE_API_KEY`)
- Модели: `OPENAI_MODEL`, `AI_CONSULTANT_MODEL`, `AI_CONSULTANT_TEMPERATURE`
- Путь к базе знаний для консультанта: `KNOWLEDGE_BASE_PATH`

Полный перечень с подсказками: `ENV_SETUP.md` и `.env.example`.

### 2. Создайте сессию Telethon (один раз)
```bash
python init_session.py
```
Скрипт использует значения из `.env`, запросит код из Telegram и создаст файл `{TELEGRAM_SESSION_NAME}.session`.

### 3. Файлы сессий
- Файл сохраняется как `{TELEGRAM_SESSION_NAME}.session` в корне проекта
- Переиспользуется всеми скриптами (`read.py`, `tg.py`, `chat_analyzer.py`, боты)
- Храните файл приватно, не коммитьте в git
- Запускайте скрипты внутри `SessionLock()` (см. `telegram_session.py`), чтобы исключить параллельное использование сессии

## 💻 Использование

### Чтение истории чата
```bash
python read.py [chat_name]
```
Примеры:
```bash
python read.py chat_alpha
python read.py chat_beta
python read.py chat_gamma
```
Доступные чаты настроены в `config.yml`.

### Простой экспорт
```bash
python tg.py [chat_name]
```

### Запуск автоответчика
```bash
python autoanswer.py
```

### AI-анализ чатов (LLM)
```bash
python chat_analyzer.py @channel_name --provider openai --limit 1000 --days 30 --output-format both
# или модульно
python -m chat_analysis.analyzer @channel_name --provider openai --limit 1000 --days 30 --output-format both
```
Отчёты сохраняются в `analysis_results/` (JSON + Markdown).

### ИИ-консультант с базой знаний
```bash
python ai_project_consultant.py --mode interactive   # консоль
python ai_project_consultant.py --mode telegram      # бот
```
Консультант подтягивает контекст из `knowledge_base/` (Markdown-файлы).

### Специализированные боты (MySQL)

- **BFL Sales Bot** (`bfl_sales_bot.py`) — AI-продавец массажных кресел (этапы воронки, возражения, подбор моделей).
- **Credit Expert Bot** (`credit_expert_bot.py`) — консультант по долгам, цель — получить телефон и назначить звонок.

Добавьте в `.env`: `BFL_SALES_BOT_TOKEN` или `CREDIT_EXPERT_BOT_TOKEN` и MySQL: `MYSQL_HOST`/`MYSQL_PORT`/`MYSQL_DATABASE`/`MYSQL_USER`/`MYSQL_PASSWORD`. Нужны таблицы `bot_users`, `bot_sessions`, `bot_messages` (DDL см. README).

Запуск:
```bash
python bfl_sales_bot.py
python credit_expert_bot.py
```

### Мониторинг и бэкапы N8N
```bash
python n8n_monitor.py          # следит за /healthz и перезапускает при падении
python n8n_backup.py backup    # создаёт архивы с workflow
```

### LightRAG анализ чатов
```bash
# Индексация сообщений из MySQL в граф знаний
python analyze_with_lightrag.py --index --limit 3000

# Поиск по графу знаний
python analyze_with_lightrag.py --query "кто ищет массажные кресла?" --mode hybrid
python analyze_with_lightrag.py --query "какие темы обсуждались?" --mode global
```
Требуются `OPENAI_API_KEY` и `MYSQL_*` из `.env`. Индексы и граф хранятся в `./lightrag_telegram`.

### MCP сервер для Telegram
```bash
python mcp_telegram_server.py
```
FastMCP сервер с тулзами `list_configured_chats`, `fetch_recent_messages`, `send_message`. Использует Telethon-сессию и алиасы из `config.yml`; лимиты управляются `MCP_TELEGRAM_LIMIT`/`MCP_TELEGRAM_MAX_LIMIT`/`MCP_TELEGRAM_LOCK_RETRY`.

### Qdrant (векторная БД)
```bash
# Запуск (systemd)
systemctl start qdrant

# Проверка
curl http://localhost:6333/collections
```

## 📝 Конфигурация чатов

Чаты настраиваются в `config.yml`:
```yaml
chats:
  example_channel:
    type: channel
    id: 1234567890
  example_user:
    type: username
    username: example_name
```

## 🎯 Ключевые функции

### Отслеживание реакций
- Подсчёт всех реакций на сообщение
- Извлечение эмодзи-реакций
- Фильтрация по уровню вовлечённости

### Обработка медиа
- Скачивание медиа из популярных сообщений
- Сохранение в директории чата
- Возможность пропуска медиа в выводе

### Фильтрация сообщений
- Автоудаление сообщений без реакций (настраивается)
- Пропуск ответов на другие сообщения
- Удаление определённых паттернов (например, Zoom-ссылок)

### AI-интеграция
- Интеграция с OpenAI GPT моделями
- Настраиваемые системные инструкции
- Генерация ответов в реальном времени

## 🧩 Общие модули
- `telegram_session.py` — проверяет наличие `.session`, даёт `SessionLock` для последовательных запусков.
- `chat_export_utils.py` — единый код для имён отправителей, реакции, сборки markdown и загрузки `config.yml` (используют `read.py`, `tg.py`, скачивание чатов).

## ⚙️ Поддержка окружений

Определение GitHub Actions и адаптация поведения:
- Уменьшение лимита до 1000 сообщений в CI/CD
- Пропуск скачивания медиа в автоматизированных окружениях

## 🔐 Безопасность

- Файлы сессий содержат токены авторизации — храните их приватно
- API-ключи должны храниться безопасно
- OpenAI API-ключи в продакшене — только через переменные окружения

## 📝 Системные промпты

**Все системные промпты должны храниться в каталоге `prompts/` в формате Markdown.**

### Правила работы с промптами

1. **Формат**: Markdown (`.md`)
2. **Расположение**: каталог `prompts/`
3. **Именование**: snake_case (например, `sales_agent.md`)
4. **Не дублировать**: промпты не должны быть зашиты в коде

### Доступные промпты

| Файл | Назначение |
|------|------------|
| `sales_agent.md` | Продающий агент (SPIN, AIDA) |
| `moderator.md` | Модератор чата |
| `digest.md` | Создатель дайджестов |
| `crm_parser.md` | Парсер CRM данных |
| `calculator.md` | Калькулятор |
| `friendly_ai.md` | Дружелюбный AI-ассистент |

### Загрузка промптов

**Python:**
```python
from integrations.prompts import Prompt, load_prompt

# Загрузка по enum
prompt = load_prompt(Prompt.SALES_AGENT)

# С контекстом
prompt = load_prompt(Prompt.SALES_AGENT, context="Продаём курсы по Python")
```

**Rust:**
```rust
use telegram_reader::{Prompt, load_prompt};

// Загрузка по enum
let prompt = Prompt::SalesAgent.load()?;

// По имени файла
let prompt = load_prompt("sales_agent.md")?;
```

### Добавление нового промпта

1. Создайте файл `prompts/new_prompt.md`
2. Добавьте enum в `integrations/prompts.py` (Python)
3. Добавьте enum в `rust_telegram/src/prompts.rs` (Rust)

## 📊 Формат вывода

Экспорт чатов сохраняется как markdown-файлы:
```
[timestamp] [sender_name]: [message_text] [reactions] [media_path]
```
Пример:
```
01.10.2025 12:30:45 UserA: Привет всем! 🔥❤️👍
UserB: Отличный пост! 🎉
```

## 🛠 Разработка

### Тестирование
```bash
pytest
# BDD
behave
```

### Создание виртуального окружения (Windows)
```bash
create_venv.cmd
```

## 📌 Примечания

- Лимит сообщений по умолчанию: 3000 (1000 в GitHub Actions)
- Порог скачивания медиа: 100,000 реакций
- Файлы сессий переиспользуются между запусками
- Поддержка личных чатов и каналов

## 🤝 Участие в разработке
Это персональный проект автоматизации. Используйте как референс для своих задач автоматизации Telegram.

## ⚖️ Лицензия
Персональный проект — используйте на своё усмотрение. Соблюдайте ToS Telegram.

---

## 📁 Файлы проекта

### Python-скрипты
| Файл | Описание |
|------|----------|
| [read.py](read.py) | Экспорт чатов в markdown (до 3000 сообщений) |
| [tg.py](tg.py) | Упрощённый экспорт чата (200 сообщений по умолчанию) |
| [autoanswer.py](autoanswer.py) | AI-автоответчик с OpenAI интеграцией |
| [init_session.py](init_session.py) | Инициализация новой Telegram-сессии |
| [list_chats.py](list_chats.py) | Список всех чатов аккаунта |
| [get_active_chats.py](get_active_chats.py) | Активные чаты с непрочитанными сообщениями |
| [export_chat.py](export_chat.py) | Экспорт конкретного чата |
| [download_chat.py](download_chat.py) | Скачивание медиа из чата |
| [download_user_chat.py](download_user_chat.py) | Скачивание личной переписки |
| [delete_zoom_messages.py](delete_zoom_messages.py) | Удаление Zoom-ссылок из чатов |
| [read_vibecod3rs.py](read_vibecod3rs.py) | Быстрый вывод последних сообщений @vibecod3rs |
| [linear_bot.py](linear_bot.py) | Бот интеграции с Linear |
| [linear_client.py](linear_client.py) | Клиент Linear API |
| [telegram_session.py](telegram_session.py) | Управление сессией Telethon |
| [chat_export_utils.py](chat_export_utils.py) | Единые утилиты: имена отправителей, реакции, markdown-экспорт, загрузка config.yml |
| [ai_project_consultant.py](ai_project_consultant.py) | AI-консультант с RAG и Telegram-бот |
| [chat_analyzer.py](chat_analyzer.py) | LLM-анализ чатов с отчётами (JSON + Markdown) |
| [task_assistant_bot.py](task_assistant_bot.py) | Бот для управления N8N/бэкапами + встроенный AI |
| [n8n_monitor.py](n8n_monitor.py) | Мониторинг и автоперезапуск N8N |
| [n8n_backup.py](n8n_backup.py) | Бэкап/restore и ротация для N8N |
| [analyze_with_lightrag.py](analyze_with_lightrag.py) | LightRAG индексация и поиск по чатам |
| [load_messages_to_db.py](load_messages_to_db.py) | Загрузка сообщений из Telegram в MySQL |
| [mcp_telegram_server.py](mcp_telegram_server.py) | FastMCP сервер для Telegram |

### Конфигурация и зависимости
| Файл | Описание |
|------|----------|
| [config.yml](config.yml) | Основная конфигурация (API, чаты, лимиты) |
| [pyproject.toml](pyproject.toml) | Конфигурация Python-проекта |
| [requirements.txt](requirements.txt) | Python-зависимости |
| [uv.lock](uv.lock) | Лок файл зависимостей uv |

### Сервисные и вспомогательные файлы
| Файл | Описание |
|------|----------|
| [n8n_monitor.service](n8n_monitor.service) | systemd unit для запуска `n8n_monitor.py` |
| [n8n_backup_cron.sh](n8n_backup_cron.sh) | Cron-обёртка для ежедневных бэкапов N8N |

### Экспортированные чаты (markdown)
Чаты сохраняются в формате `{chat_name}.md` в корне проекта или в `chats/`.
Чат-файлы добавлены в `.gitignore` для защиты приватности.

### Документация
| Файл | Описание |
|------|----------|
| [README.md](README.md) | Основная документация проекта |
| [AGENTS.md](AGENTS.md) | Описание AI-агентов и общая справка |
| [CLAUDE.md](CLAUDE.md) | Инструкции для Claude (этот файл) |
| [DASHA_TOOLS.md](DASHA_TOOLS.md) | Гайд по N8N мониторингу, бэкапам и AI-консультанту |
| [ENV_SETUP.md](ENV_SETUP.md) | Подробная настройка `.env` |
| [CONFIGURATION_SUMMARY.md](CONFIGURATION_SUMMARY.md) | Резюме по вынесению конфигурации в `.env` |
| [SECURITY_CLEANUP.md](SECURITY_CLEANUP.md) | Проверка очистки чувствительных данных |
| [codev/protocols/spider-solo/protocol.md](codev/protocols/spider-solo/protocol.md) | Активный протокол разработки |
| [codev/specs/](codev/specs/) | Спецификации проектов |
| [codev/plans/](codev/plans/) | Планы реализации |
| [codev/reviews/](codev/reviews/) | Код-ревью |
| [codev/resources/](codev/resources/) | Ресурсы и вспомогательные материалы |
| [.claude/agents/](.claude/agents/) | Агентные инструкции для Claude |

### Rust-версия (`rust_telegram/`)
| Файл | Описание |
|------|----------|
| [rust_telegram/Cargo.toml](rust_telegram/Cargo.toml) | Rust-зависимости |
| [rust_telegram/src/main.rs](rust_telegram/src/main.rs) | CLI точка входа |
| [rust_telegram/src/config.rs](rust_telegram/src/config.rs) | Загрузка config.yml |
| [rust_telegram/src/session.rs](rust_telegram/src/session.rs) | Управление Telegram-сессией |
| [rust_telegram/src/commands/read.rs](rust_telegram/src/commands/read.rs) | Чтение чатов |
| [rust_telegram/src/commands/send_message.rs](rust_telegram/src/commands/send_message.rs) | Отправка сообщений |
| [rust_telegram/src/commands/init_session.rs](rust_telegram/src/commands/init_session.rs) | Инициализация сессии |
| [rust_telegram/src/commands/list_chats.rs](rust_telegram/src/commands/list_chats.rs) | Список чатов |
| [rust_telegram/src/commands/export.rs](rust_telegram/src/commands/export.rs) | Экспорт чатов |
| [rust_telegram/src/commands/tg.rs](rust_telegram/src/commands/tg.rs) | Быстрый экспорт |
| [rust_telegram/src/commands/autoanswer.rs](rust_telegram/src/commands/autoanswer.rs) | AI-автоответчик |
| [rust_telegram/src/commands/delete_zoom.rs](rust_telegram/src/commands/delete_zoom.rs) | Удаление Zoom |

### Claude-агенты
| Файл | Описание |
|------|----------|
| [.claude/agents/architecture-documenter.md](.claude/agents/architecture-documenter.md) | Документирование архитектуры |
| [.claude/agents/codev-updater.md](.claude/agents/codev-updater.md) | Обновление Codev |
| [.claude/agents/spider-protocol-updater.md](.claude/agents/spider-protocol-updater.md) | Обновление SPIDER протокола |

## Методология Codev

Проект использует методологию разработки Codev (контекстно-ориентированная разработка).

### Активный протокол
- Протокол: SPIDER-SOLO (вариант для одного разработчика)
- Расположение: codev/protocols/spider-solo/protocol.md

### Структура директорий
- Спецификации: codev/specs/
- Планы: codev/plans/
- Ревью: codev/reviews/
- Ресурсы: codev/resources/

### Агенты рабочего процесса
Доступны в `.claude/agents/`:
- `spider-protocol-updater` — анализирует реализации SPIDER и даёт рекомендации по улучшению
- `architecture-documenter` — поддерживает документацию архитектуры
- `codev-updater` — обновляет Codev до последней версии

См. codev/protocols/spider-solo/protocol.md для полной документации протокола.

---

## 📋 Задачи и проекты

### Инфраструктура
- **n8n** — настроить и установить платформу автоматизации процессов
- **Comet** — разобраться, как оплатить браузер от Perplexity

### AI-проекты
- **ИИ ассистент** — разработка персонального AI-помощника
- **Голосовой ИИ продавец** — автоматизация продаж с голосовым AI
- **Локальная LLM для STT/TTS** — преобразование текст↔голос

### Связка для голосового продавца
- **LLM 7-8B** (DeepSeek 8B / LLaMA 8B / Qwen 7B, 4-bit quant) — достаточно для голосового продавца
- **STT/TTS локально** (Silero или аналог), без внешних аудио-API

### Мощности под локальные модели

**Для 5 параллельных разговоров:**
- Реалистичный таргет: ответ робота ≤ 1 сек, 5 активных диалогов одновременно

**Железо (минимально комфортно):**
- **GPU:** RTX 4070 / 4070 Super, 12 ГБ VRAM — для 7-8B-модели в 4-битном квантовании + KV-кэш для 5 сессий
- **CPU:** 8-12 ядер (16-24 потока)

### Локальные LLM для голоса
- **Whisper** (OpenAI) — STT, локальный, работает офлайн
- **Silero** — STT/TTS, быстрый, на русском
- **Coqui TTS** — open source TTS
- **Bark** — генерация голоса от Suno
- **XTTS** — multilingual TTS от Coqui
- **Piper** — быстрый локальный TTS

### Идеи проектов
- Telegram → Linear: превращать сообщения в задачи с автотегами
- Автодайджест: дневные/недельные резюме чатов
- Локер встреч: бот для бронирования слотов
- Платный AI-консьерж в Telegram
- «AI-ответчик для каналов» — white-label autoanswer.py
- Бот для платных слотов: запись/оплата консультаций через Telegram

---

## 📂 Категоризация чатов

Чаты организованы по категориям в `chat_topics.yml`:
- IT и программирование
- Локальные сообщества
- Бизнес и карьера
- Саморазвитие
- Развлечения

Подробная информация и ID чатов хранятся в приватном `config.yml`.
